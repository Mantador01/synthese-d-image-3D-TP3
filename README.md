Ce dépôt contient le projet du TP3.
Le code C++ se trouve dans `tp.cpp`.

Pour compiler :
```bash
premake4 gmake
make tp
bin/tp
```
ça va donner la version par défaut que j'ai set comme la version avec 1 rebond indirect éclairage, sinon voir a la fin du rapport j'ai lister les commandes !
